// Backwards-compat shim: re-export the new ImportAllModal component so
// consumers that still import `ImportCsvModal` continue to work.
import ImportAllModal from './ImportAllModal'
export default ImportAllModal
